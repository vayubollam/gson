<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <import type="android.view.View"></import>
        <variable
            name="stationItem"
            type="suncor.com.android.ui.home.stationlocator.StationItem"></variable>
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:orientation="vertical" android:layout_width="match_parent"
        android:paddingLeft="16dp"
        android:background="@color/white"

        android:layout_height="wrap_content">
        <androidx.appcompat.widget.AppCompatTextView
            android:layout_width="wrap_content"
            android:id="@+id/txt_nearby_title"
            style="@style/SearchTextNormal"
            tools:text="22 Indian db"
            android:layout_height="wrap_content"
            android:text="@{stationItem.station.address.addressLine}"
            android:layout_marginTop="14dp"
            />
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <androidx.appcompat.widget.AppCompatTextView
                android:layout_width="wrap_content"
                android:id="@+id/txt_nearby_distance_duration"
                android:text="@{@string/distance_generic(stationItem.distanceDuration.distance, stationItem.distanceDuration.duration)}"
                android:visibility="@{stationItem.distanceDuration==null ? View.INVISIBLE : View.VISIBLE}"
                tools:text="12 km away . Open until 11:00 PM"
                android:textSize="14sp"
                android:layout_height="wrap_content" />
            <ProgressBar
                android:layout_width="20dp"
                android:visibility="@{stationItem.distanceDuration!=null ? View.INVISIBLE : View.VISIBLE}"
                android:layout_height="20dp" />
        </RelativeLayout>
        <androidx.appcompat.widget.AppCompatImageView
            android:layout_width="match_parent"
            android:src="@drawable/horizontal_divider"
            android:layout_marginTop="14dp"
            android:layout_height="wrap_content" />
    </LinearLayout>
</layout>


